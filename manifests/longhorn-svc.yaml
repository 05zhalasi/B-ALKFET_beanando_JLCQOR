apiVersion: v1
kind: Service
metadata:
  name: longhorn-dashboard-lb
  namespace: longhorn-system
spec:
  selector:
    app: longhorn-ui
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
